<!DOCTYPE html>
<html lang="en">

<head>
    <title>Borga Web App</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/bootstrap.min.css" rel="stylesheet">
    <script src="/bootstrap.bundle.min.js" type="text/javascript"></script>
</head>

<body>

    <div class="p-5 bg-primary text-white text-center">
        <h1>Borga Web App</h1>
        <p>Web App to create groups of games using ATLAS Api</p>
    </div>

    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Home</a>
                </li>
                {{#if username}}
                <li class="nav-item">
                    <a class="nav-link" href="/users/{{username}}">UserInfo</a>
                </li>
                {{/if}}
                <li class="nav-item">
                    <a class="nav-link" href="/populargames?limit=50">PopularGames</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/about">About</a>
                </li>
            </ul>
        </div>
        {{#if username}}
            <h1 style="color:red;">{{username}}&nbsp;&nbsp;</h1>              
            <form class="d-flex" method="post" action="/logout">
                <button class="btn btn-outline-warning" type="submit">Logout</button>
            </form>
        {{else}}
            <form class="d-flex" method="get" action="/signup">
                <button class="btn btn-outline-warning" type="submit">Signup</button>
            </form>
            <form class="d-flex" method="get" action="/login">
                <button class="btn btn-outline-warning" type="submit">Login</button>
            </form>
        {{/if}}
    </nav>
 


    <div class="container mt-5">
        <div id="alertPanel">
            {{#if alert}}
                <div class="alert alert-{{alert.kind}} alert-dismissible fade show" role="alert">
                    <strong>{{alert.title}}</strong>
                    {{alert.message}}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {{/if}}
        </div>
        <div class="row">
            {{{body}}}
        </div>
    </div>
</body>

</html>